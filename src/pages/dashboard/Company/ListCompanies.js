import { Grid, Typography } from "@mui/material";
import React from "react";

const ListCompanies = () => {
  return (
    <div>
      <Typography variant="h4" gutterBottom>
        List Companies
      </Typography>

      <Grid container margin={25} spacing={3} sx={{}}></Grid>
    </div>
  );
};

export default ListCompanies;
